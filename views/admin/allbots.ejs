<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Bots</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', sans-serif;
        }

        body {
            display: flex;
            height: 100vh;
            background-color: #f0f2f5;
            color: #333;
        }

        .sidebar {
            width: 250px;
            background: #1e272e;
            color: white;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        .sidebar h2 {
            font-size: 22px;
            margin-bottom: 30px;
            text-align: center;
            border-bottom: 1px solid #485460;
            padding-bottom: 10px;
        }

        .sidebar a {
            color: #dcdde1;
            text-decoration: none;
            padding: 12px;
            border-radius: 6px;
            margin: 6px 0;
            display: flex;
            align-items: center;
            transition: background 0.2s;
        }

        .sidebar a:hover {
            background: #485460;
        }

        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        .header {
            font-size: 26px;
            font-weight: 600;
            margin-bottom: 20px;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .create-bot-btn {
            background-color: #1e90ff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            cursor: pointer;
            text-decoration: none;
        }

        .bot-list {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .bot-card {
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
            width: calc(33.333% - 20px);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            transition: transform 0.2s;
            text-decoration: none;
            color: #2f3542;
        }

        .bot-card:hover {
            transform: translateY(-5px);
            background-color: #f1f2f6;
        }

        .bot-card img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-bottom: 10px;
        }

        .bot-title {
            font-size: 18px;
            font-weight: bold;
        }

        .bot-meta {
            font-size: 14px;
            color: #555;
            margin-top: 8px;
        }

        @media (max-width: 768px) {
            .bot-card {
                width: 100%;
            }
        }
    </style>
</head>

<body>

    <div class="sidebar">
        <h2>Admin Panel</h2>
        <a href="/admin">🏠 Home</a>
        <a href="/admin/chats">💬 Chats</a>
        <a href="/admin/analytics">📊 Analytics</a>
        <a href="/admin/settings/bots">⚙️ Settings</a>
        <a href="#">🚪 Logout</a>
    </div>

    <div class="main-content">
        <div class="top-bar">
            <div class="header">🤖 All Bots</div>
            <a href="/admin/settings/bot/create" class="create-bot-btn">➕ Create New Bot</a>
        </div>

        <div class="bot-list">
            <!-- Example: This part should be populated dynamically from your backend -->
            <% bots.forEach(bot=> { %>
                <a class="bot-card" href="/admin/settings/bot/<%= bot._id %>">
                    <img src="<%= bot.avtar %>" alt="Bot Avatar" />
                    <div class="bot-title">
                        <%= bot.title %>
                    </div>
                    <div class="bot-meta">Status: <%= bot.status %>
                    </div>
                    <div class="bot-meta">Greeting: "<%= bot.greetingMessage %>"</div>
                </a>
                <% }) %>
        </div>
    </div>
</body>

</html>